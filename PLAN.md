# Plan — MAWS orchestrator

1. **Done.** Named agents wrapping unified `crc_score` / `zg_score` / `infra_score` / `decide` / `rpa_suggest`.
2. **Done.** Supervisor `iter_maws`; unified `iter_flow` delegates here.
3. **Done.** Hive demo :8874; automate seven stories; stay-on-blue compensation event.
4. **Later.** Scorecard outcomes as the paper’s “learning” signal. No LLM mesh. No auto-apply.

Do not add GenSecOps AEO. Do not let MAWS override DSA.
